7 Five of a kind
6 Four of a kind
5 Full house
4 Three of a kind
3 Two pair
2 One pair
1 High card

        if len(j_positions) >= 1:
            for a in j_positions:
                if len(j_positions) >= 2:
                    for b in j_positions:
                        if len(j_positions) >= 3:
                            for c in j_positions:
                                if len(j_positions) >= 4:
                                    for d in j_positions:
                                        if len(j_positions) >= 5:
                                            hand.hand_type = 7